---
title: "Tool Selection & Optimization"
description: Hierarchical routing, context-based filtering, and analytics to improve tool selection accuracy
sidebarTitle: "Tool Selection & Optimization"
icon: book
mode: wide
slug: agents/tool-selection-and-optimization
---
import { CodeEditor } from '/snippets/CodeEditor.jsx';
import { Quiz, QuizQuestion } from '/snippets/Quiz.jsx';

Agent accuracy drops from 92% to 58% as tool count grows from 5 to 20+. This page covers hierarchical routing, context-based filtering, and tool analytics.

## The Tool Selection Problem

**Scenario:** You give your agent 20 tools. The agent uses wrong ones constantly.

**Research shows:** Agent accuracy decreases with tool count:
- 1-5 tools: 92% correct selection
- 6-10 tools: 84% correct selection
- 11-20 tools: 71% correct selection
- 20+ tools: 58% correct selection

**Why:** LLMs pattern-match descriptions. Large option spaces overwhelm them.

## Challenge 1: Too Many Tools

The anti-pattern: dumping all tools into a flat list. The agent sees 20 options and guesses.

<CodeEditor
  file="src/agent-reliability/tool_selection_patterns.ts"
  lines="22-28"
  functionName="flatToolList"
  title="Anti-Pattern: Flat Tool List (20 tools → 58% accuracy)"
/>

### Solution 1: Hierarchical Routing

Two-step routing: first pick domain + action, then call the specific tool. Agent sees 1 routing tool instead of 20 → accuracy jumps to 90%+.

<CodeEditor
  file="src/agent-reliability/tool_selection_patterns.ts"
  lines="38-65"
  functionName="createHierarchicalRouter"
  title="Hierarchical Routing (2 steps → 90%+ accuracy)"
/>

### Solution 2: Context-Based Tool Groups

Return only relevant tools for the current conversation phase. Agent sees 2-4 tools at a time.

<CodeEditor
  file="src/agent-reliability/tool_selection_patterns.ts"
  lines="75-89"
  functionName="getToolsForPhase"
  title="Context-Based Tool Groups (phase filtering)"
/>

## Challenge 2: Overlapping Functionality

Three tools named `search_products`, `find_products`, `product_lookup` — all do the same thing. The agent picks randomly.

### Solution: Clear Differentiation

Each tool has a distinct purpose with "Use when" / "Do NOT use" guidance:

<CodeEditor
  file="src/agent-reliability/tool_selection_patterns.ts"
  lines="99-146"
  functionName="createDifferentiatedProductTools"
  title="Clear Tool Differentiation (distinct purposes)"
/>

The agent now picks the right tool for each query type:
- "wireless mouse" → `search_products_by_text` (fuzzy text search)
- "PROD-001" → `get_product_by_sku` (exact lookup)
- "mice under $20" → `filter_products_by_attributes` (structured filter)

## Advanced: Tool Usage Analytics

Track every tool call to find unused tools, high-failure tools, and latency bottlenecks:

<CodeEditor
  file="src/agent-reliability/tool_analytics.ts"
  lines="17-77"
  functionName="ToolAnalytics"
  title="Tool Analytics: Metrics & Recommendations"
/>

Run this in production to get recommendations like:
- "Remove unused tool: `legacy_search`"
- "`flaky_tool` fails 40% — review error handling"
- "`slow_api` averages 3000ms — consider caching"

<Quiz>
<QuizQuestion question="Your agent has 25 tools and selects the wrong one 40% of the time. What's the most impactful fix?" options={["Improve the system prompt with better instructions", "Reduce to 5 tools using hierarchical routing or context-based groups", "Switch to a larger, more capable model"]} answer={1} explanation="Tool count is the #1 factor in selection accuracy. Reducing from 25 to 5 tools (via routing or phase filtering) typically improves accuracy from ~58% to 90%+." />
<QuizQuestion question="You have search_products and find_products tools that do the same thing. The agent calls both on every query. What's the fix?" options={["Remove one and keep the other", "Add 'Do NOT use' guidance to each description explaining when to use the other", "Rename them to be more different"]} answer={1} explanation="The agent needs clear boundaries. 'Use when / Do NOT use' descriptions eliminate ambiguity. Simply renaming doesn't help if the descriptions still overlap." />
</Quiz>
